# LaosTravel environment template.
# Backend/auth/payment providers are intentionally not selected yet.
# Never commit real secrets.
NEXT_PUBLIC_API_BASE_URL=

# Server-only provider adapter variables (names stay generic until provider selection).
DATABASE_URL=
AUTH_ISSUER_URL=
AUTH_AUDIENCE=
OBJECT_STORAGE_ENDPOINT=
OBJECT_STORAGE_BUCKET=

# Booking safety gate. Keep false until auth, authoritative quote verification,
# PostgreSQL adapter wiring and database integration CI are verified.
BOOKING_HOLD_MINUTES=
BOOKING_API_APPROVED=false
