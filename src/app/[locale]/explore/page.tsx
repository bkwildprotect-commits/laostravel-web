import { ServiceDiscovery } from "@/components/ServiceDiscovery";import type { Locale } from "@/i18n/locales";export default async function ExplorePage({params}:{params:Promise<{locale:string}>}){const {locale}=await params;return <main className="container discoveryPage"><p className="eyebrow">DISCOVER LAOS</p><h1>Explore Vang Vieng</h1><p className="discoveryLead">Browse LaosTravel service categories for the pilot area.</p><ServiceDiscovery locale={locale as Locale}/><div className="emptyState"><strong>Choose a service category</strong><p>Search results will use verified partner data from the shared LaosTravel backend.</p></div></main>}