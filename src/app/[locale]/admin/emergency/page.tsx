import { AdminAccessBlocked } from "@/components/AdminAccessBlocked";import { requireAdminAccess } from "@/lib/auth/admin";
export default async function EmergencyAdmin(){const access=await requireAdminAccess();if(!access.authorized)return <AdminAccessBlocked/>;return <main className="container adminPage"><p className="eyebrow">ADMIN · EMERGENCY CONTACTS</p><h1>Verified emergency contacts</h1><div className="emptyState"><strong>No contacts loaded</strong><p>Production records will require area, contact type, name, phone, priority, active status and verified date. Unverified numbers must not be published to travellers.</p></div></main>}
