import Link from "next/link";import type { Locale } from "@/i18n/locales";export default async function PartnerPage({params}:{params:Promise<{locale:string}>}){const {locale}=await params;const l=locale as Locale;return <main className="container partnerLanding"><p className="eyebrow">LAOSTRAVEL PARTNER</p><h1>Grow with LaosTravel</h1><p>Manage your services, availability and bookings through one partner workspace. Partner applications are reviewed before verification.</p><div className="partnerActions"><Link className="primaryButton" href={"/"+l+"/partner/apply"}>Become a Partner</Link><Link className="ghostControl" href={"/"+l+"/partner/dashboard"}>Partner Dashboard preview</Link></div></main>}