import type { Locale } from "@/i18n/locales";
export default async function ServiceDetail({params}:{params:Promise<{locale:string;serviceId:string}>}){const {locale,serviceId}=await params;void locale;return <main className="container detailPage"><p className="eyebrow">SERVICE DETAIL</p><h1>Service details</h1><p className="discoveryLead">Service ID: {serviceId}</p><div className="detailGrid"><section className="detailMain"><div className="detailMedia" aria-label="Service photos placeholder"></div><h2>Description & location</h2><p>Verified partner content, translations, photos and reviews will load from the shared backend.</p></section><aside className="bookingCard"><h2>Check availability</h2><button>Date</button><button>Guests / options</button><div className="pricePlaceholder">Price calculated by server</div><button className="primaryButton" disabled>Continue to booking</button><small>Booking stays disabled until real availability and authoritative pricing are connected.</small></aside></div></main>}
