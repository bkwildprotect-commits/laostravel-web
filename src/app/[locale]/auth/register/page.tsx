import Link from "next/link";import { AuthShell } from "@/components/AuthShell";import { AuthForm } from "@/components/AuthForm";import type { Locale } from "@/i18n/locales";
export default async function Register({params}:{params:Promise<{locale:string}>}){const {locale}=await params;const l=locale as Locale;return <AuthShell locale={l} title="Create your LaosTravel account" subtitle="One account for bookings, trips, saved places, rewards and partner access."><AuthForm mode="register"/><p className="authFinePrint">By creating an account, you will be asked to accept the current Terms and Privacy Policy when those policies are published.</p><div className="authLinks"><Link href={"/"+l+"/auth/login"}>Already have an account? Sign in</Link></div></AuthShell>}
