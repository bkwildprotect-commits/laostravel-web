import Link from "next/link";import { AuthShell } from "@/components/AuthShell";import { AuthForm } from "@/components/AuthForm";import type { Locale } from "@/i18n/locales";
export default async function Login({params}:{params:Promise<{locale:string}>}){const {locale}=await params;const l=locale as Locale;return <AuthShell locale={l} title="Sign in" subtitle="Use your LaosTravel account across web and mobile."><AuthForm mode="login"/><div className="authLinks"><Link href={"/"+l+"/auth/forgot-password"}>Forgot password?</Link><Link href={"/"+l+"/auth/register"}>Create an account</Link></div></AuthShell>}
