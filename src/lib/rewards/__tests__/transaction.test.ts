import {describe,it,expect,vi} from "vitest";import {applyBookingRewards} from "../transaction";
const tx={} as never;
function repo(points=BigInt(100),coupon:"CLAIMED"|"ALREADY_USED"="CLAIMED"){return {lockUserPoints:vi.fn(),getAvailablePoints:vi.fn(async()=>points),redeemPoints:vi.fn(),claimCoupon:vi.fn(async()=>coupon)}}
describe("booking rewards transaction",()=>{it("locks points before redeeming",async()=>{const r=repo();await applyBookingRewards(tx,r,{userId:"u",bookingId:"b",pointsToRedeem:50});expect(r.lockUserPoints).toHaveBeenCalledBefore(r.redeemPoints)});it("rejects insufficient points",async()=>{await expect(applyBookingRewards(tx,repo(BigInt(10)),{userId:"u",bookingId:"b",pointsToRedeem:50})).rejects.toThrow("INSUFFICIENT_POINTS")});it("rejects duplicate coupon claim",async()=>{await expect(applyBookingRewards(tx,repo(BigInt(100),"ALREADY_USED"),{userId:"u",bookingId:"b",couponCode:"save",couponBenefitAmount:"100"})).rejects.toThrow("COUPON_ALREADY_USED")});});
