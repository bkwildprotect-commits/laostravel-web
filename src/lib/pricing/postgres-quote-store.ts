import type {TransactionContext} from "@/lib/infrastructure/transaction";import type {AuthoritativeQuote,QuoteStore} from "./quote-verification";
export class PostgresQuoteStore implements QuoteStore{
 constructor(private tx:TransactionContext){}
 async getAuthoritativeQuote(id:string):Promise<AuthoritativeQuote|null>{const r=await this.tx.query<{id:string;service_id:string;availability_id:string;option_id:string|null;service_date:string;quantity:number;availability_token:string;currency:string;base_amount:string;fees_amount:string;coupon_amount:string;points_benefit_amount:string;customer_total:string;expires_at:string}>("SELECT id,service_id,availability_id,option_id,service_date::text,quantity,availability_token,currency,base_amount::text,fees_amount::text,coupon_amount::text,points_benefit_amount::text,customer_total::text,expires_at::text FROM price_quotes WHERE id=$1 FOR SHARE",[id]);const q=r[0];if(!q)return null;return {serviceId:q.service_id,availabilityId:q.availability_id,date:q.service_date,quantity:q.quantity,...(q.option_id?{optionId:q.option_id}:{}),availabilityToken:q.availability_token,priceQuoteId:q.id,currency:q.currency,baseAmount:q.base_amount,feesAmount:q.fees_amount,couponAmount:q.coupon_amount,pointsBenefitAmount:q.points_benefit_amount,customerTotal:q.customer_total,expiresAt:q.expires_at}}
}
