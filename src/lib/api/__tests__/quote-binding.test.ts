import {describe,it,expect} from "vitest";import {verifyQuoteBinding} from "../quote-binding";import type {AvailabilityQuote,CreateBookingRequest} from "../contracts";
const q:AvailabilityQuote={serviceId:"s1",date:"2030-01-01",quantity:2,availabilityToken:"a",priceQuoteId:"p",currency:"LAK",baseAmount:"100",feesAmount:"0",couponAmount:"0",pointsBenefitAmount:"0",customerTotal:"100",expiresAt:"2030-01-01T01:00:00Z"};
const b:CreateBookingRequest={serviceId:"s1",availabilityToken:"a",priceQuoteId:"p",quantity:2,traveller:{name:"A",email:"a@b.com"},idempotencyKey:"12345678"};
describe("quote booking binding",()=>{it("accepts matching unexpired quote",()=>expect(verifyQuoteBinding(q,b,new Date("2030-01-01T00:00:00Z"))).toEqual({ok:true}));it("rejects expired quote",()=>expect(verifyQuoteBinding(q,b,new Date("2030-01-01T02:00:00Z"))).toEqual({ok:false,code:"QUOTE_EXPIRED"}));it("rejects changed quantity",()=>expect(verifyQuoteBinding(q,{...b,quantity:3},new Date("2030-01-01T00:00:00Z"))).toEqual({ok:false,code:"QUOTE_MISMATCH"}));});
