import {describe,it,expect} from "vitest";import {decideSubmission,normalizeSubmissionKey} from "../submission-idempotency";import {checkRateWindow} from "../rate-limit-policy";
describe("tour lead submission protection",()=>{it("rejects weak idempotency keys",()=>expect(()=>normalizeSubmissionKey("short")).toThrow("INVALID_SUBMISSION_KEY"));it("replays completed duplicate",()=>expect(decideSubmission({key:"abcdefghijklmnop",requestHash:"h",status:"COMPLETED",expiresAt:200},{key:"abcdefghijklmnop",requestHash:"h",now:100})).toBe("REPLAY"));it("rejects changed payload under same key",()=>expect(()=>decideSubmission({key:"abcdefghijklmnop",requestHash:"h1",status:"PROCESSING",expiresAt:200},{key:"abcdefghijklmnop",requestHash:"h2",now:100})).toThrow());it("blocks over-limit window",()=>expect(checkRateWindow({count:5,resetAt:200},{now:100,limit:5,windowMs:60000}).allowed).toBe(false));});
