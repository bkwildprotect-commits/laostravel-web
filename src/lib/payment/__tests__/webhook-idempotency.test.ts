import {describe,it,expect} from "vitest";import {decideWebhook} from "../webhook-idempotency";
const x={provider:"gateway",eventId:"evt1",payloadHash:"hash",status:"COMPLETED" as const};
describe("payment webhook idempotency",()=>{it("processes unseen event",()=>expect(decideWebhook(undefined,{provider:"gateway",eventId:"evt1",payloadHash:"hash"})).toBe("PROCESS"));it("replays completed duplicate without side effects",()=>expect(decideWebhook(x,{provider:"gateway",eventId:"evt1",payloadHash:"hash"})).toBe("REPLAY"));it("rejects same event id with changed payload",()=>expect(()=>decideWebhook(x,{provider:"gateway",eventId:"evt1",payloadHash:"changed"})).toThrow("WEBHOOK_PAYLOAD_CONFLICT"));it("blocks concurrent processing",()=>expect(decideWebhook({...x,status:"PROCESSING"},{provider:"gateway",eventId:"evt1",payloadHash:"hash"})).toBe("IN_PROGRESS"));});
