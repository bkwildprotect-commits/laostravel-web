const modules=[["partners","Partner Verification"],["services","Services Moderation"],["bookings","Booking Operations"],["reviews","Reviews"],["rewards","Coupons & Coins"],["emergency","Emergency Contacts"],["audit","Audit & Security"]];
export function AdminDashboard(){return <div className="adminDashboard"><aside><h2>LaosTravel Admin</h2><nav>{modules.map(([id,label])=><a key={id} href={"#"+id}>{label}</a>)}</nav></aside><section className="adminWorkspace"><p className="eyebrow">OPERATIONS</p><h1>Admin Dashboard</h1><p className="adminLead">Operational controls require authenticated, authorized admin access before any real data or actions are enabled.</p><div className="adminGrid">{modules.map(([id,label])=><article id={id} key={id} className="adminCard"><div><strong>{label}</strong><span>Not connected</span></div><p>{id==="partners"?"Human review of submitted partner applications and verification evidence.":id==="services"?"Review service submissions before activation.":id==="bookings"?"Support booking issues without silently rewriting transaction history.":id==="reviews"?"Moderate booking-linked reviews with auditable actions.":id==="rewards"?"Manage rule-driven coupons and ledger-based Coins; no direct partner balance editing.":id==="emergency"?"Maintain verified contact numbers by area, priority and verification date.":"Record sensitive operational actions for accountability and security review."}</p><button disabled>Backend required</button></article>)}</div></section></div>}
