"use client";import { FormEvent,useState } from "react";
type Mode="login"|"register"|"forgot";
export function AuthForm({mode}:{mode:Mode}){const [message,setMessage]=useState("");function submit(e:FormEvent){e.preventDefault();setMessage("Authentication provider is not connected yet. Your details were not sent or stored.")}return <form className="authForm" onSubmit={submit}>{mode==="register"&&<label>Full name<input name="name" autoComplete="name" required/></label>}<label>Email<input type="email" name="email" autoComplete="email" required/></label>{mode!=="forgot"&&<label>Password<input type="password" name="password" autoComplete={mode==="login"?"current-password":"new-password"} minLength={8} required/></label>}<button className="primaryButton" type="submit">{mode==="login"?"Sign in":mode==="register"?"Create account":"Send recovery instructions"}</button>{message&&<p className="formNotice" role="status">{message}</p>}</form>}
