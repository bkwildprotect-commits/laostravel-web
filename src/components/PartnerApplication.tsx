"use client";import { FormEvent,useState } from "react";import { partnerCategories,type PartnerCategory } from "@/lib/partner/model";
const names:Record<PartnerCategory,string>={hotel:"Hotel",restaurant:"Restaurant",attraction:"Attraction","tour-activity":"Tour / Activity",transport:"Transport","car-rental":"Car Rental",guide:"Guide / Freelancer"};
export function PartnerApplication(){const [notice,setNotice]=useState("");function submit(e:FormEvent){e.preventDefault();setNotice("Preview only. No application or identity document was uploaded because the secure backend and verification storage are not connected yet.")}return <form className="partnerForm" onSubmit={submit}><label>Business category<select required defaultValue=""><option value="" disabled>Select category</option>{partnerCategories.map(c=><option key={c} value={c}>{names[c]}</option>)}</select></label><label>Business / professional name<input required/></label><label>Responsible contact name<input required autoComplete="name"/></label><div className="formTwo"><label>Email<input type="email" required autoComplete="email"/></label><label>Phone<input type="tel" required autoComplete="tel"/></label></div><label>Operating area<input defaultValue="Vang Vieng"/></label><div className="verificationNote"><strong>Verification</strong><p>Required evidence will depend on partner type and current legal/operational requirements. Sensitive documents will use private storage and authorized review only.</p></div><button className="primaryButton" type="submit">Continue application preview</button>{notice&&<p className="formNotice" role="status">{notice}</p>}</form>}
