import Link from "next/link";import type { Locale } from "@/i18n/locales";import { serviceCategories } from "@/lib/services/catalog";
export function ServiceDiscovery({locale,active}:{locale:Locale;active?:string}){return <><nav className="categoryTabs" aria-label="Service categories">{serviceCategories.map(c=><Link className={active===c.slug?"active":""} key={c.slug} href={"/"+locale+"/explore/"+c.slug}>{c.name}</Link>)}</nav><section className="discoveryPanel"><div><label className="filterLabel">Location<button>Vang Vieng</button></label><label className="filterLabel">Date<button>Select date</button></label></div><button className="primaryButton">Search</button></section></>}
