import Link from "next/link";import type { Locale } from "@/i18n/locales";import { messages } from "@/i18n/messages";
export function AccountNav({locale}:{locale:Locale}){const a=messages[locale].account;const items=[["profile",a.profile],["trips",a.trips],["saved",a.saved],["coupons",a.coupons],["coins",a.coins],["reviews",a.reviews],["settings",a.settings]];return <nav className="accountNav" aria-label={a.label}>{items.map(([path,label])=><Link key={path} href={"/"+locale+"/"+path}>{label}</Link>)}</nav>}
