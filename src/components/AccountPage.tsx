import { AccountNav } from "./AccountNav";import type { Locale } from "@/i18n/locales";
export function AccountPage({locale,title,description,children}:{locale:Locale;title:string;description:string;children?:React.ReactNode}){return <main className="container accountPage"><aside><h2>My LaosTravel</h2><AccountNav locale={locale}/></aside><section className="accountContent"><h1>{title}</h1><p className="accountLead">{description}</p>{children??<div className="emptyState"><strong>No data yet</strong><p>Your real account data will appear here after secure authentication and backend services are connected.</p></div>}</section></main>}
