import {spawn} from "node:child_process";
const url=process.env.DATABASE_URL;if(!url){console.error("DATABASE_URL is required for disposable PostgreSQL bootstrap testing");process.exit(2)}
function psql(args=[]){return new Promise(resolve=>{const p=spawn("psql",[url,"-v","ON_ERROR_STOP=1",...args],{stdio:["ignore","pipe","pipe"]});let out="",err="";p.stdout.on("data",d=>out+=d);p.stderr.on("data",d=>err+=d);p.on("close",code=>resolve({code,out,err}))})}
for(const file of ["docs/backend/database-schema.sql","db/migrations/0001_core_booking.sql","db/migrations/0002_rewards_integrity.sql","db/migrations/0003_payment_webhook_idempotency.sql","db/migrations/0004_commercial_path_exclusivity.sql","db/migrations/0005_price_quotes.sql","db/migrations/0006_price_snapshot_quote.sql","db/migrations/0007_quote_consumption.sql","db/migrations/0008_auth_identities.sql"]){const r=await psql(["-f",file]);if(r.code!==0){console.error("Bootstrap failed:",file,r.err);process.exit(1)}}
const checks=["SELECT to_regclass('public.booking_idempotency') IS NOT NULL","SELECT to_regclass('public.partner_booking_commercial_paths') IS NOT NULL","SELECT to_regclass('public.payment_webhook_events') IS NOT NULL","SELECT to_regclass('public.price_quotes') IS NOT NULL","SELECT to_regclass('public.auth_identities') IS NOT NULL","SELECT EXISTS (SELECT 1 FROM information_schema.columns WHERE table_schema='public' AND table_name='price_quotes' AND column_name='consumed_at')","SELECT EXISTS (SELECT 1 FROM information_schema.columns WHERE table_schema='public' AND table_name='price_quotes' AND column_name='consumed_booking_id')"];
for(const sql of checks){const r=await psql(["-Atc",sql]);if(r.code!==0||r.out.trim()!=="t"){console.error("Schema invariant failed:",sql,r);process.exit(1)}}
console.log("PASS: fresh reference schema plus migrations 0001-0008 bootstrapped successfully");
